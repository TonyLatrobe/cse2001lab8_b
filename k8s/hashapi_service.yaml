apiVersion: v1
kind: Service
metadata:
  name: hash-api
  namespace: staging
  labels:
    app: hash-api
    team: platform
spec:
  selector:
    app: hash-api
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP